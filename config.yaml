targets:
  # groups / contacts below are NAMES or PHONES, resolved via the whapi API.
  groups: []
  communities: []
  channels: []
  contacts: []
  # ids: raw chat IDs used directly — NO API resolution (0 requests).
  ids:
    - "120363298579412558@g.us"   # Thora photos dedo 🤳🏻 (group)
    - "918799507812"              # contact (matched in both forms)
    - "919512843906"              # contact

ingestion:
  capture_events: ["post", "put", "delete", "status"]
  include_outgoing: true

backfill:
  enabled: false              # set true to enable history catch-up (uses API quota)
  interval_seconds: 7200
  per_chat_page_size: 100
  initial_history_pages: 5

media:
  max_concurrent_downloads: 3
  download_jitter_ms: [100, 500]
  retry_attempts: 3
